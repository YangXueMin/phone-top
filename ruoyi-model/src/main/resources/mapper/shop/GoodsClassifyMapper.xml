<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.shop.mapper.GoodsClassifyMapper">

    <resultMap type="GoodsClassify" id="GoodsClassifyResult">
        <result property="classId"    column="class_id"    />
        <result property="parentId"    column="parent_id"    />
        <result property="ancestors"    column="ancestors"    />
        <result property="name"    column="name"    />
        <result property="picture"    column="picture"    />
        <result property="orderNum"    column="order_num"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectGoodsClassifyVo">
        select
            class_id,
            parent_id,
            ancestors,
            name,
            picture,
            order_num,
            del_flag,
            create_by,
            create_time,
            update_by,
            update_time
        from shop_goods_classify
    </sql>

    <select id="selectGoodsClassifyList" parameterType="GoodsClassify" resultMap="GoodsClassifyResult">
        <include refid="selectGoodsClassifyVo"/>
        <where>
            <if test="parentId != null "> and parent_id = #{parentId}</if>
            <if test="ancestors != null  and ancestors != ''"> and ancestors = #{ancestors}</if>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="orderNum != null "> and order_num = #{orderNum}</if>
        </where>
        order by order_num DESC
    </select>

    <select id="selectGoodsClassifyByClassId" parameterType="Long" resultMap="GoodsClassifyResult">
        <include refid="selectGoodsClassifyVo"/>
        where class_id = #{classId}
    </select>

    <select id="hasChildByClassId" parameterType="Long" resultType="int">
        select count(1) from shop_goods_classify
        where del_flag = '0' and parent_id = #{classId} limit 1
    </select>

    <select id="checkNameUnique" resultMap="GoodsClassifyResult">
        <include refid="selectGoodsClassifyVo"/>
        where name=#{name} and parent_id = #{parentId} and del_flag = '0' limit 1
    </select>

    <insert id="insertGoodsClassify" parameterType="GoodsClassify" useGeneratedKeys="true" keyProperty="classId">
        insert into shop_goods_classify
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="parentId != null">parent_id,</if>
            <if test="ancestors != null">ancestors,</if>
            <if test="name != null">name,</if>
            <if test="picture != null">picture,</if>
            <if test="orderNum != null">order_num,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="parentId != null">#{parentId},</if>
            <if test="ancestors != null">#{ancestors},</if>
            <if test="name != null">#{name},</if>
            <if test="picture != null">#{picture},</if>
            <if test="orderNum != null">#{orderNum},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateGoodsClassify" parameterType="GoodsClassify">
        update shop_goods_classify
        <trim prefix="SET" suffixOverrides=",">
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="ancestors != null">ancestors = #{ancestors},</if>
            <if test="name != null">name = #{name},</if>
            <if test="picture != null">picture = #{picture},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where class_id = #{classId}
    </update>

    <delete id="deleteGoodsClassifyByClassId" parameterType="Long">
        delete from shop_goods_classify where class_id = #{classId}
    </delete>

    <select id="checkClassifyExistGoods" parameterType="Long" resultType="int">
        select count(1) from shop_goods where class_id = #{classId}
    </select>

    <select id="checkClassifyExistCompanyGoods" parameterType="Long" resultType="int">
        select count(1) from shop_company_goods where class_id = #{classId}
    </select>

    <delete id="deleteGoodsClassifyByClassIds" parameterType="String">
        delete from shop_goods_classify where class_id in
        <foreach item="classId" collection="array" open="(" separator="," close=")">
            #{classId}
        </foreach>
    </delete>
</mapper>